sonarqube {
    properties {
        property 'sonar.host.url', 'https://sonarcloud.io'
        property 'sonar.organization', "${project.group}"
        property 'sonar.projectKey', "${project.group}_${rootProject.name}"
    }
}
